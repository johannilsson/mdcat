[package]
name = "kitty-pager"
version = "0.1.0"
edition = "2021"
description = "A terminal pager with Kitty graphics protocol support for inline images"
license = "MIT"

[dependencies]
crossterm = "0.28"
base64 = "0.22"
anyhow = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
